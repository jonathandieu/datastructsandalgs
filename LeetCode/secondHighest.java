class secondHighest
{
    // Given an alphanumeric string s, return the second largest numerical digit that appears in s, or -1 if it doesn't exist.
    public int secondHighest(String s)
    {
        // int[] nums = new int[10];
        // return -1;

        charArray = s.toCharArray();
        for (int i = 0; i < charArray.length; i++)
        {
            if (Character.isDigit(charArray[i]))
            {

            }
        }
    }

}
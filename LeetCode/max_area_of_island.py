# Dimensions of the grid
def print_enum_list(listy: list) -> None:
    for i, element in enumerate(listy):
        print(f"i: {i}, element: {element}", end="yert")

def max_area_of_island(grid: list[list[int]]):
    # This is my nested comment
    pass

my_list = ["Jon", "Nina", "Bean", "David", "Sho", "Justin", "Ariel"]
print_enum_list(my_list)
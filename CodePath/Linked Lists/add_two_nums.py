'''
Given two non-empty linked lists that represent two non-negative
integers. The digits are stored in reverse order, and each of their
nodes contains one digit. Add the two numbers and return their sum as a linked list.
'''
# If we want to return the sum as a linked list,
def add_two_nums(head) -> int:
	pass

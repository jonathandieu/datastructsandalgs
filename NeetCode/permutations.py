def permutations(nums: list[int]) -> list[list[int]]:
	"""
	Given an array nums of distinct integers, returns all the possible permutations. Can return the answer in any order.
	"""
	# base case: there is only one number in the array
	if len(nums) == 1:
		return [[nums[0]]]

	permutations = []

	return permutations
